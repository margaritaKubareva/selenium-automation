package lvAcodemy.page_object;

import org.junit.Test;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertThrows;

public class MyTest<HttpClientErrorException> {

        private static final String BASE_URL = "http://acodemy-app-springboot-env.eba-pagku2yg.eu-north-1.elasticbeanstalk.com/";
    private Object HttpStatus;

//    @Test
//        public void testEmptyData() {
//            RestTemplate restTemplate = new RestTemplate();
//            String url = BASE_URL + "/your-endpoint"; // Замените "/your-endpoint" на реальный путь к вашему эндпоинту
//
//            HttpClientErrorException exception = assertThrows(HttpClientErrorException.class, () -> {
//                restTemplate.postForEntity(url, null, String.class);
//            });
//
//            assertEquals(exception.getClass(), HttpStatus.getClass());
//        }
    }
